# common

Shared test utilities and helper functions used across integration test files. This module provides reusable test infrastructure for creating mock CSV files, setting up test environments, and common assertion helpers to reduce duplication in the test suite.
