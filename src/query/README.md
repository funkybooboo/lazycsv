# query

SQL query mode using in-memory SQLite for executing queries with multi-table JOIN support. This module allows users to run SQL queries against CSV files by loading them into SQLite tables, supporting complex operations like joins, aggregations, and filtering across multiple files.
