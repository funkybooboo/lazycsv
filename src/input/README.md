# input

Central input handling system that routes keyboard events to appropriate mode-specific handlers. This module serves as the dispatcher for all user input, determining which mode (normal, insert, visual, magnifier, SQL) is active and delegating key events to the correct handler.
