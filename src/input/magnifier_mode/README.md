# magnifier_mode

Input handlers for magnifier mode including vim motions, operators, search, and mode transitions. This module processes keyboard input when the magnifier (full vim editor) is active, translating key presses into editor commands and state changes.
