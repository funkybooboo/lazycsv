# search

Regex-based search functionality with automatic literal fallback, wrap-around navigation, and match highlighting. This module provides powerful search capabilities that attempt regex matching first and fall back to literal substring search, allowing users to find and navigate to matching cells.
