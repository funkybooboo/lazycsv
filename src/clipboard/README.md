# clipboard

Triple clipboard system with independent buffers for row, column, and rectangular region operations. This module provides separate clipboard functionality for different visual selection modes, allowing users to copy and paste rows, columns, or arbitrary rectangular cell ranges.
