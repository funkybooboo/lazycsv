# session

Multi-file session manager handling CSV file switching and maintaining parsing configuration (delimiters, headers, encoding). This module tracks all loaded CSV files, manages the current active file, and preserves settings like delimiter and header options across file switches.
