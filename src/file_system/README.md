# file_system

CSV file discovery and filesystem operations for multi-file session management. This module scans directories to find CSV files, handles file path resolution, and supports switching between multiple CSV files within a single session.
