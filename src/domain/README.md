# domain

Core domain types providing type-safe position wrappers (RowIndex, ColIndex) to prevent coordinate confusion. This module defines fundamental types that enforce correctness at compile time, ensuring row indices can't be accidentally used as column indices and vice versa.
