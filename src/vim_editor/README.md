# vim_editor

Reusable vim editor engine providing modal editing, motions, operators, visual selection, and undo/redo. This module implements a complete vim editing system that can be embedded in different contexts, serving as the foundation for both magnifier mode and other vim-style editing features.
